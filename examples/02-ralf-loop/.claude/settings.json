{
  "hooks": {
    "Stop": [
      {
        "matcher": "",
        "hooks": [
          {
            "type": "command",
            "command": "bash -c 'cd \"$CLAUDE_PROJECT_DIR\" && if ! git diff --name-only HEAD -- . | grep -q \"\\.py$\"; then exit 0; fi && python -m pytest tests/ -v 2>&1 | tail -30'",
            "timeout": 30000
          }
        ]
      }
    ]
  },
  "permissions": {
    "deny": [
      "Edit(.claude/settings.json)",
      "Write(.claude/settings.json)",
      "Edit(tests/**)",
      "Write(tests/**)"
    ]
  }
}

---
paths:
  - api/**
---

# API Module Rules

All Python files in `api/` MUST have the suffix `_endpoint` in their filename.
Examples: `user_endpoint.py`, `order_endpoint.py`, `health_endpoint.py`.

@docs/api-design.md
